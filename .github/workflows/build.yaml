name: Build, push, and deploy Docker images
on:
  push:
    branches:
      - "main"
jobs:
  push_to_registry:
    name: Build and push Docker images to ghcr.io
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Check out the repo
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build images
        run: docker compose -f compose.prod.yaml build

      - name: Push images
        run: docker compose -f compose.prod.yaml push

  deploy:
    name: Deploy to production via SSH
    runs-on: ubuntu-latest
    needs: push_to_registry
    environment: 
      name: production
      url: https://kvak.idebate.net
    steps:
      - name: Check out the repo
        uses: actions/checkout@v6

      - name: Set up SSH key
        run: |
          install -m 600 -D /dev/null "${{ github.workspace }}/.ssh/id_ed25519"
          echo "${{ secrets.DEPLOY_SSH_KEY }}" > "${{ github.workspace }}/.ssh/id_ed25519"

      - name: Write .env file
        run: |
          cat > .env <<'EOF'
          POSTGRES_DB_PASSWORD=${{ secrets.POSTGRES_DB_PASSWORD }}
          DJANGO_SECRET_KEY=${{ secrets.DJANGO_SECRET_KEY }}
          APP_PORT=127.0.0.1:8001
          EOF

      - name: Deploy with Docker Compose
        env:
          DOCKER_HOST: ssh://${{ vars.DEPLOY_SSH_USER }}@${{ secrets.DEPLOY_SSH_HOST }}/run/user/${{ vars.DEPLOY_SSH_UID }}/podman/podman.sock
          COMPOSE_PROJECT_NAME: kvak
        run: |
          docker compose -f compose.prod.yaml up -d --remove-orphans --pull always
